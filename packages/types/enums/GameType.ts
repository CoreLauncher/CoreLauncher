export enum GameType {
	/**
	 * The game has a single set of settings and instance
	 */
	Normal = "normal",

	/**
	 * The game can have multiple instances that each have their own settings
	 */
	Instanced = "instanced",
}
